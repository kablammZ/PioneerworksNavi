<h1><%= @event.name %></h1>

<p><%= @event.description %></p>

<p>Starts: <%= @event.start_date.in_time_zone("EST").strftime("%A, %B %d, %Y") %><br>
<%= @event.start_date.in_time_zone("EST").strftime("%l:%M %p") %></p>

<p>Ends: <%= @event.end_date.in_time_zone("EST").strftime("%A, %B %d, %Y") %><br>
<%= @event.end_date.in_time_zone("EST").strftime("%l:%M %p") %></p>

<%= link_to 'Edit event info', edit_event_path(@event) %> |
<%= link_to 'Remove event', event_path(@event), :method => :delete %>
